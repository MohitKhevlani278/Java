// Basic steps to develop JDBC Application

1 ) Import packages 
	
	import java.sql.* OR javax.sql.*;

2) Loading the JDBC Driver

	try 
	{
		Class.forName(DriverName);
	}
	catch(ClassNotFoundException ce)
	{
		System.out.println("Error Loading Driver");
	}

3) Connect to the database 
	getConnection() - is used to establish a connection to a database, it returns connection object. it throws SQLException.

	Connection con=getConnection("Protocol:subprotocol:Source/Dsn");


4) create and executing statement
	
	createStatement() 
	
		Statement st=con.createStatement();
		ResultSet rs=st.executeQuery(Query);

5) Processing data returned by the dbms.
		 boolean rs.next() 		1 - abc
		 getXXX()
		 getString()			2 - def		

6) Terminate the connection to the database.
	st.close();
	con.close();
		 

